# 貢献の仕方

## 原則を変えるとき

1. `principles/` の該当ファイルを編集する。構成は `principles/_TEMPLATE.md` に従う。
2. 数値を変えるなら `assumptions.yaml` を直し、`updated_at` を更新する。本文の数値は yaml のキー名で参照する。
3. `cases/` の関連ケースで期待回答が崩れないか確認する。必要ならケースを追加する。
4. `python3 scripts/build_lite.py` を実行して `dist/lite.md` を再生成し、7,000字（スクリプトの上限）を超えていないか確認する。超えたら追加分を圧縮する。

## 書き方

- 読者はAI。知識ではなく「何を確認し、どの順で考え、何を言ってはいけないか」を書く。
- 「買うべき」「買わないべき」どちらにも寄らない。
- 書籍からの逐語引用は行わない。考え方を自分の言葉で書く。
- 数字には単位を付ける。

## 議論

Issue で。「この質問にAIがこう答えた」という実例つきの報告がいちばん助かります。

## ライセンスと利用条件

- 貢献した文章は CC BY-SA 4.0、コードは MIT License で公開されます。
- あわせて、貢献した内容（文章・数値・コード・Issue での提案を含む）を株式会社TERASSが商用利用できることに同意したものとみなします。TERASSのサービス・アプリ・AI機能・資料への組み込みや、公開ライセンスとは異なる条件での提供を含みます。詳細は [CLA.md](CLA.md) を見てください。
- Pull Request を送る、Issue に投稿する時点で、上記に同意したものとみなします。
